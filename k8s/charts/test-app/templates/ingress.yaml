apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: main-ingress
  annotations:
    cert-manager.io/cluster-issuer: "clusterissuer-letsencrypt"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - {{ .Values.hosts.app-1 }}
    - {{ .Values.hosts.app-2 }}
    secretName: app-tls
  rules:
   - host: {{ .Values.hosshosts.app-1 }}
     http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: kuber-service-1
                port: 
                  number: 80
   - host: {{ .Values.hosts.app-2 }}
     http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: kuber-service-2
                port: 
                  number: 80
